{
  "name": "intedata",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typegen": "pnpm -C packages/api typegen"
  },
  "devDependencies": {
    "turbo": "^2.1.1",
    "typescript": "^5.5.4"
  }
}
